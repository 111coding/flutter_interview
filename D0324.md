# 면접스터디 0324
- 방식 : 면접관 3 - 피면접자 1

## 동준님
- 메인면접관 : 영광님
### 질문
- 영광님 : HTTP의 특징과 문제점? 대칭키 사용..?! 보안 취약 한걸로 알고있음. 통신하기 위한 프로토콜. 약속같은거.
    - HTTP의 동작 과정? 요청하면 서버에서 응답받고... 잘모르겠음.
    - HTTPS? HPPT에 암호화를 얹은거.
    - 암호화 방식? 잘 모르겠음
- 혜림님 : JIT vs AOT? AOT는 미리 컴파일 되기 때문에 실행할 때 동작빠름. JIT 실행될때마다 컴파일 되기때문에 동작 느림
    - Flutter에서는? 빌드, 릴리즈 다름
    - JIT 디버그 모드에서 버벅이는 이유?
- 지원 : 프로토콜? 약속. 통신할 때 약속
### 피드백
- 영광님 : 모르는 부분에서 자신감 있으면 좋을듯
- 혜림님 : 답변 좋았음
- 지원 : 자신감

## 영광님
- 메인면접관 : 혜림님
### 질문
- 혜림님 : HTTPS? HTTP 단점 보완. 예전엔 SSL, 최근엔 TLS
    - SSL/TLS 차이? 모르겠음
    - 대칭키 비대칭키 암호화? 비대칭키 여는열쇠 잠그는 열쇠 따로 있음.
- 동준님 : Flutter에서 상태? UI에서 직접적으로 보이는 데이터(바뀌는)
    - Riverpod 특징? Notifier, NotifierProvder
    - Riverpod vs Provider? 같은개발자, 알파벳순서바꾼거(애너그램). 전역적인 상태관리는 같지만 위젯트리에 의존적이라는 단점
- 지원 : 상태관리 패키지 종류와 특징? 대표적 Riverpod 가장 많이, GetX, Blox
### 피드백
- 혜림님 : 설명 Good
- 동준님 : 설명 Good
- 지원 : SSL/TLS 학습. CS 넓게 보충

## 혜림님
- 메인면접관 : 동준님
### 질문
- 동준님 : 정렬알고리즘 종류 및 사용이유? 탐색속도. 사용자 입장에서도 빨라서 원활하게 동작
    - 선택정렬, 버블정렬? 버블 O, 선택 잘모르겠음.
- 영광님 : 캐시메모리? 잘 모르겠음
    - 가상메모리? 실행에 필요한 것만 메모리에 올릴 수 있음
    - 메모리 4가지 영역? Stack, Heap...
    - Stack vs Heap? 스택은 커다란 창고. Heap은 상대적으로 느림. Heap은 가비지컬렉터 사용.
- 지원 : 코드가 화면에 보이기까지 과정? 사용자가 화면 터치하면 위젯 생성... 생명주기... 위젯트리 엘리먼트트리 랜더트리...
### 피드백
- 동준님 : 보충 학습 필요
- 영광님 : 메모리 보충 학습 필요. 비슷한 용어 맥락에 따라서 해석 필요
- 지원 : GPT랑 대화하는데 이전 대화내용을 기억못하는 느낌. 전체적인 이론들을 하나로 연결!